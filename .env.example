# App Name
NEXT_PUBLIC_APP_NAME="ssss"

# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgres://avnadmin:ssss-nlkTQ9ATZ@sssss-fm-nanocrypt-1d3d.j.aivencloud.com:13134/defaultdb?sslmode=require&connection_limit=1&pool_timeout=20"

# Gemini API key (free tier) - get one at https://aistudio.google.com/app/apikey
GEMINI_API_KEY="AQ.Ab8RN6IKpsssFG6hvIx-s"

# ─── Privy (Auth + Wallet) ────────────────────────────────────────────────────
# Get from https://dashboard.privy.io — see privy_step.txt
NEXT_PUBLIC_PRIVY_APP_ID="ssss"

# ─── Solana ───────────────────────────────────────────────────────────────────
# Treasury wallet: Solana address that receives gacha spin payments
NEXT_PUBLIC_SOLANA_TREASURY="ssss"
# RPC URL (devnet for testing, mainnet-beta for production)
NEXT_PUBLIC_SOLANA_RPC_URL="https://api.devnet.solana.com"

# ─── Vercel Cron ──────────────────────────────────────────────────────────────
# Any random string — add same value to Vercel env vars
# dapat dari sini ya Do : node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET="ssss"


# ─── Pusher (Real-time) ───────────────────────────────────────────────────────
# Get from https://pusher.com → Create app → App Keys
PUSHER_APP_ID="ss"
PUSHER_KEY="sss"
PUSHER_SECRET="ss"
PUSHER_CLUSTER="ap1"
NEXT_PUBLIC_PUSHER_KEY="ssss"     # same as PUSHER_KEY
NEXT_PUBLIC_PUSHER_CLUSTER="ap1"             # same as PUSHER_CLUSTER

# ─── Upstash Redis ────────────────────────────────────────────────────────────
# Get from https://console.upstash.com → Create Database → REST API
UPSTASH_REDIS_REST_URL="https://optimal-emu-128503.upstash.io"
UPSTASH_REDIS_REST_TOKEN="ssss"

